import { AmxGridFilter } from './grid-filter.directive';

describe('GridFilterDirective', () => {
    it('should create an instance', () => {
        const directive = new AmxGridFilter();
        expect(directive).toBeTruthy();
    });
});
