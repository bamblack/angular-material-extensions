import { AmxGridFiltersContainer } from './grid-filters-container.directive';

describe('GridFiltersContainerDirective', () => {
    it('should create an instance', () => {
        const directive = new AmxGridFiltersContainer();
        expect(directive).toBeTruthy();
    });
});
